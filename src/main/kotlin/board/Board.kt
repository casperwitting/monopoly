package board

import board.Space

class Board {
    var spaces : MutableList<Space> = mutableListOf()

//    fun move(token: Token) {
//        val nextSpace = spaces.get(token.position + 1)
//
//
//    }
}
